<VirtualHost *:80>
    ServerName dashboard.alaress.com.au

    RewriteEngine on
    RewriteCond %{REQUEST_URI} !^/.well-known/.*
    RewriteRule /(.*) https://dashboard.alaress.com.au/$1 [R,L]
</VirtualHost>
